node_modules/
target/
dist/
*.node
.env
.env.*
.env-*
.DS_Store
.turbo/
coverage/
*.log

# App-local working data
# sqlite writes -shm and -wal beside the file in WAL mode, so match the family
data/*.db*
data/storage/attachments/

# Vite/Photon build output
public/build/
